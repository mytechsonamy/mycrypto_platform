# Application
NODE_ENV=development
PORT=3001

# Database
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=exchange_dev
DATABASE_USER=exchange_user
DATABASE_PASSWORD=securepassword123

# JWT Configuration - RSA Keys (RS256)
# Use RSA keys for production. Generate with:
#   openssl genrsa -out private.pem 4096
#   openssl rsa -in private.pem -pubout -out public.pem
# Then store in AWS Secrets Manager or secure location
JWT_ALGORITHM=RS256
JWT_PRIVATE_KEY_PATH=/app/keys/private.pem
JWT_PUBLIC_KEY_PATH=/app/keys/public.pem
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=30d
JWT_EMAIL_VERIFICATION_EXPIRY=24h

# Deprecated - kept only for backwards compatibility with legacy systems
# DO NOT USE FOR NEW IMPLEMENTATIONS
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# RabbitMQ
RABBITMQ_URL=amqp://guest:guest@localhost:5672
RABBITMQ_EMAIL_QUEUE=email.verification

# Rate Limiting
RATE_LIMIT_TTL=3600
RATE_LIMIT_LIMIT=5

# Argon2
ARGON2_TIME_COST=12
ARGON2_MEMORY_COST=65536
ARGON2_PARALLELISM=1

# =============================================================================
# RECAPTCHA CONFIGURATION (Google reCAPTCHA v3)
# =============================================================================

# Site key: Used on frontend to identify the site to reCAPTCHA
# Secret key: Used on backend to verify reCAPTCHA tokens with Google API
#
# Development: Use test keys below (always pass verification)
# Production: Generate keys from https://www.google.com/recaptcha/admin/create
RECAPTCHA_SITE_KEY=6LeIxAcTAAAAAJcZVRqyHh71UMIEGNQ_MXjiZKhI
RECAPTCHA_SECRET_KEY=6LeIxAcTAAAAAGG-vFI1TnRWxMZNFuojJ4WifJWe

# Score threshold for reCAPTCHA v3 (0.0 to 1.0)
# Higher score = more likely to be human
# Recommended: 0.5 (balanced security)
# Adjust based on your tolerance for bot traffic vs. user friction
RECAPTCHA_SCORE_THRESHOLD=0.5

# =============================================================================
# TRADE ENGINE INTEGRATION
# =============================================================================

# Trade Engine API base URL
# Development: http://localhost:8080/api/v1
# Production: https://trade-engine.yourcompany.com/api/v1
TRADE_ENGINE_API_URL=http://localhost:8080/api/v1

# Service-to-service authentication token
# Generate a secure token for internal service communication
# In production, use AWS Secrets Manager or similar secure storage
TRADE_ENGINE_SERVICE_TOKEN=your-secure-service-token-change-in-production